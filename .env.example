# ==============================================================================
# Stock Analysis Client - Standalone Edition
# Environment Configuration Template
# ==============================================================================

# ------------------------------------------------------------------------------
# Application Mode
# ------------------------------------------------------------------------------
# Set to 'standalone' for frontend-only mode (default)
# Set to 'integrated' if connecting to a backend server
VITE_APP_MODE=standalone

# ------------------------------------------------------------------------------
# Data Source Configuration
# ------------------------------------------------------------------------------
# Options: 'mock' (use local JSON files) or 'api' (use external API)
VITE_DATA_SOURCE=mock

# ------------------------------------------------------------------------------
# API Configuration (Only needed if VITE_DATA_SOURCE=api)
# ------------------------------------------------------------------------------
# Backend API base URL (uncomment and configure if using external API)
# VITE_API_BASE_URL=http://localhost:9001

# WebSocket URL for real-time features (uncomment if needed)
# VITE_WEBSOCKET_URL=http://localhost:9001

# API request timeout in milliseconds
# VITE_API_TIMEOUT=30000

# ------------------------------------------------------------------------------
# Feature Flags
# ------------------------------------------------------------------------------
# Enable/disable authentication (set to false for standalone mode)
VITE_ENABLE_AUTH=false

# Enable/disable real-time updates via WebSocket
VITE_ENABLE_REAL_TIME=false

# Enable/disable portfolio features
VITE_ENABLE_PORTFOLIO=true

# Enable/disable advanced charting
VITE_ENABLE_ADVANCED_CHARTS=true

# Enable/disable dark mode
VITE_ENABLE_DARK_MODE=true

# ------------------------------------------------------------------------------
# Data Refresh Settings
# ------------------------------------------------------------------------------
# Auto-refresh interval for stock data (in milliseconds)
# Default: 60000 (1 minute), set to 0 to disable auto-refresh
VITE_AUTO_REFRESH_INTERVAL=60000

# Cache duration for API responses (in milliseconds)
# Default: 300000 (5 minutes)
VITE_CACHE_DURATION=300000

# ------------------------------------------------------------------------------
# UI Configuration
# ------------------------------------------------------------------------------
# Default number of items per page
VITE_ITEMS_PER_PAGE=20

# Default theme ('light', 'dark', or 'system')
VITE_DEFAULT_THEME=system

# Default language ('ko' for Korean, 'en' for English)
VITE_DEFAULT_LANGUAGE=ko

# ------------------------------------------------------------------------------
# Analytics & Monitoring (Optional)
# ------------------------------------------------------------------------------
# Google Analytics ID (uncomment if using)
# VITE_GA_TRACKING_ID=

# Sentry DSN for error tracking (uncomment if using)
# VITE_SENTRY_DSN=

# ------------------------------------------------------------------------------
# External API Keys (Only if integrating with external services)
# ------------------------------------------------------------------------------
# These are only needed if you're using external data providers
# DO NOT commit these values to version control!

# OpenAI API key (for AI-powered analysis features)
# VITE_OPENAI_API_KEY=

# News API key (for real-time news)
# VITE_NEWS_API_KEY=

# ------------------------------------------------------------------------------
# Development Settings
# ------------------------------------------------------------------------------
# Enable debug mode (shows additional console logs)
VITE_DEBUG_MODE=false

# Enable mock delays (simulates API latency for testing)
VITE_ENABLE_MOCK_DELAYS=false

# Mock API delay in milliseconds
VITE_MOCK_DELAY=500

# ------------------------------------------------------------------------------
# Build Configuration
# ------------------------------------------------------------------------------
# Base URL for production deployment (e.g., '/stock-client/' for GitHub Pages)
# VITE_BASE_URL=/

# Enable source maps in production
# VITE_ENABLE_SOURCE_MAPS=false

# ==============================================================================
# Usage Instructions
# ==============================================================================
# 1. Copy this file to .env in the project root
#    cp .env.example .env
#
# 2. Update the values according to your needs
#
# 3. Restart the development server after making changes
#    pnpm dev
#
# Note: Environment variables starting with VITE_ are exposed to the client.
# Never store sensitive information in client-side environment variables!
# ==============================================================================
